Options -Indexes
RewriteEngine On

# Remove .php extension — /api/destinations → destinations.php
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.php -f
RewriteRule ^([^\.]+)$ $1.php [NC,L]
